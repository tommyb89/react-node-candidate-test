import React from 'react';

import './Button.css';

const Button = (): JSX.Element => {
	return (
		<div className='btn-primary-apply'>
			<button>Apply</button>
		</div>
	);
};

export default Button;
